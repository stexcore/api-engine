/**
 * Symbol to register a creating service
 */
export const RegisterCreatingServiceSymbol = Symbol("register.creating.service");

/**
 * Symbol to unregister creating service symbol
 */
export const UnregisterCreatingServiceSymbol = Symbol("unregister.creating.service");